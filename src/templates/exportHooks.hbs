{{>header}}

import * as Types from './data-types{{>tsExtention}}';
import { createSwrHook } from '{{{factories}}}';

{{#each services}}
{{#each operations}}
export const use{{{capitalize name}}} = createSwrHook<Types.{{{../name}}}{{{capitalize name}}}Input, Types.{{{../name}}}{{{capitalize name}}}Output>({
	route: '{{{path}}}',
	method: '{{{method}}}'{{#if parametersBody}}{{#if parametersBody.mediaType}},
	mediaType: '{{{parametersBody.mediaType}}}'{{/if}}{{/if}},
});

{{/each}}
{{/each}}
