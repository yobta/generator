{{>header}}
"use client";

import * as Types from '../data-types/types{{>tsExtention}}';
import { createClientResolver } from '{{{factories}}}';

{{#each service.operations}}
/**
{{#if deprecated}}
	* @deprecated
{{/if}}
{{#if summary}}
	* {{{escapeComment summary}}}
{{/if}}
{{#if description}}
	* {{{escapeComment description}}}
{{/if}}
{{#each results}}
	 * @returns {{{type}}} {{#if description}}{{{escapeComment description}}}{{/if}}
{{/each}}
*/
export const {{{name}}} = createClientResolver<Types.{{{@root.service.name}}}{{{capitalize name}}}Input, Types.{{{@root.service.name}}}{{{capitalize name}}}Output>({
	route: '{{{path}}}',
	method: '{{{method}}}'{{#if parametersBody}}{{#if parametersBody.mediaType}},
	mediaType: '{{{parametersBody.mediaType}}}'{{/if}}{{/if}},
});
{{/each}}
