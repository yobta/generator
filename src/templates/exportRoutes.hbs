{{>header}}

{{#each services}}
{{#each operations}}
/**
{{#if deprecated}}
	* @deprecated
{{/if}}
{{#if summary}}
	* {{{escapeComment summary}}}
{{/if}}
{{#if description}}
	* {{{escapeComment description}}}
{{/if}}
	* @throws ApiError
*/
export const {{{operationName . ../. @root.services}}}Route = '{{path}}';

{{/each}}
{{/each}}
